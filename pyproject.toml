[project]
name = "twinai"
version = "0.1.0"
description = "SimPy discrete-event simulation models (BrewLine coffee-bar digital twin)"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.2",
    "gunicorn>=23.0.0",
    "python-dotenv>=1.0.1",
    "pydantic>=2.9",
    "simpy>=4.1.2",
]

[tool.uv]
package = false
